import React from 'react';

function FunctionSellerOrder() {
  return (
    <div>
      {a.map((a, index) => (
        <p
          key={ index }
        >
          a
        </p>
      ))}
    </div>
  );
}

export default FunctionSellerOrder;
